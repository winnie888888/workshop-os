{
  "name": "@workshop/shared",
  "version": "0.1.0",
  "private": true,
  "description": "Shared domain core and types (money, RBAC, audit hash-chain, numbering, tenant context). Dependency-free; consumed by API and web.",
  "type": "module",
  "main": "src/index.ts",
  "types": "src/index.ts",
  "exports": {
    ".": "./src/index.ts",
    "./*": "./src/*"
  },
  "scripts": {
    "test": "node --experimental-strip-types test/run.ts",
    "build": "tsc -p tsconfig.json"
  },
  "devDependencies": {
    "@types/node": "^22.5.0",
    "typescript": "^5.6.0"
  }
}
